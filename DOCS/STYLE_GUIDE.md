<!-- PRESERVATION RULE: Never delete or replace content. Append or annotate only. -->
# STYLE GUIDE
- Naming: camelCase (JS/TS), kebab-case (CSS)
- Trace: // [TRACE: filename.md]
- Comments: WHY only

[2026-03-29]: **Website data:** Public mod list lives in **`mods.json`** at repo root (JSON only; no comments inside the file). Naming: follow each object’s keys as documented in `DOCS/SUMMARY.md` / `DOCS/ARCHITECTURE.md`.

[AMENDED 2026-03-29]: Mod list source is **`#otterdays-mod-data`** in **`index.html`** (`type="application/json"`). Valid JSON only (no `//` comments). Keys unchanged from prior `mods.json` schema; see amended `DOCS/SUMMARY.md` / `DOCS/ARCHITECTURE.md`.
